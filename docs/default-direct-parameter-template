; Direct index variables: default parameter file template.
; Return values for tables and indices set to zero.

; <p> mag	<p> off		<p> tbl
; i<p>		<p>off		i<p>tbl

; <p> ix mag 	<p> ix off		<p> ix tbl		<p> ix phase
; i<p>ix		i<p>ixoff		i<p>ixtbl		i<p>ixphase

; <p> tbl off	<p> tbl wrap	<p> ix tbl scan period
; i<p>toff		i<p>twrap	i<p>ixsp

; table: number (specified in header.sco)
; period: n >= 0, n/duration (p3)n < 0, frequency (Hz)

; parameters
;
; n	n	tbl
; n	n	tbl	n
; n	n	n